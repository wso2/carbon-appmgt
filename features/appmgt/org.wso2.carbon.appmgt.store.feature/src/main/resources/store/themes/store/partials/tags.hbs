<div class="row">
    <div class="span3">
          <h4><i class="icon-star"></i>{{t "Tags"}}</h4>

        <div class="tags">
            <ul>{{#each .}}
                {{#each data}}
                    <li>
                        <a class="tag" href="{{url ../this.url}}{{name}}">{{name}}</a>
                    </li>
                {{/each}}
                {{/each}}
            </ul>
        </div>

    </div>
</div>